# Database (Required: Use Neon/Supabase for production)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Server
PORT=5000
NODE_ENV=production

# Admin access (Required: Set strong key for production)
ADMIN_KEY=your-strong-admin-secret-key

# Optional: PostgreSQL connection details (for local development)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=ak_detailing